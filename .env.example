# Copia questo file in .env e riempilo

# PostgreSQL — Railway lo genera automaticamente
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Ambiente
NODE_ENV=production

# Porta (Railway la imposta automaticamente)
PORT=3000
